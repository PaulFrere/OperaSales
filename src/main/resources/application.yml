server:
  port: 8081

spring:
  datasource:
    driver-class-name: 'org.postgresql.Driver'
    url: 'jdbc:postgresql://localhost:5432/opera'
    username: 'postgres'
    password: 'postgres'

jpa:
  show-sql: true